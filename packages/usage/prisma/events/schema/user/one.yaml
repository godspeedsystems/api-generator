/user/:id.http.get:
  summary: Fetch User
  description: Fetch User from database
  fn: com.biz.schema.user.one
  params:
    - name: id
      in: path
      required: true
      schema:
        type: integer
