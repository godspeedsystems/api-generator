/post/:id.http.get:
  summary: Fetch Post
  description: Fetch Post from database
  fn: com.biz.schema.post.one
  params:
    - name: id
      in: path
      required: true
      schema:
        type: integer
