summary: Update Profile
tasks:
  - id: schema_profile_update
    fn: com.gs.datastore
    args:
      datasource: schema
      data:
        where:
          id: <% std.parseInt(inputs.params.id) %>
        data: <% inputs.body.data %>
      config:
        method: Profile.update
