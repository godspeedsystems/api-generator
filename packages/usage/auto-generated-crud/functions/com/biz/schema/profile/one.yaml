summary: Fetch Profile
tasks:
  - id: schema_profile_one
    fn: com.gs.datastore
    args:
      datasource: schema
      data:
        where:
          id: <% std.parseInt(inputs.params.id) %>
      config:
        method: profile.findUnique
