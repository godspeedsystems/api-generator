summary: Update Category
tasks:
  - id: schema_category_update
    fn: com.gs.datastore
    args:
      datasource: schema
      data:
        where:
          id: <% std.parseInt(inputs.params.id) %>
        data: <% inputs.body.data %>
      config:
        method: Category.update
