/schema/comment/:id.http.delete:
  summary: Delete a Comment
  description: Delete Comment from database
  fn: com.biz.schema.comment.delete
  params:
    - name: id
      in: path
      required: true
      schema:
        type: string
  responses:
    content:
      application/json:
        schema:
          type: object
